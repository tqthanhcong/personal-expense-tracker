# AI Usage Report

Codex in ChatGPT Work was used to refine the product scope, create the initial specification commit, implement the frontend and backend, define the OpenAPI contract, and write tests. The generated application was verified by running the backend test suite and building the frontend. The scope was kept to equal expense splitting for one group so every implemented behavior could be checked directly against the specification.

